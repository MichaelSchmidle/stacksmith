# n8n Automation Configuration
N8N_HOSTNAME=auto.example.com

# Timezone (used by n8n for scheduling)
TZ=Europe/Zurich

# Note: Configuration follows official n8n Docker setup
# Uses SQLite database by default (no PostgreSQL required)
# GENERIC_TIMEZONE is used by n8n for proper timezone handling